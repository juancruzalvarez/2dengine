#version 460 core
layout (location = 0) in vec3 vertex_pos;
layout (location = 1) in vec2 uvs;

uniform mat4 ortho;
uniform vec2 pos;

out vec2 uv_coords; 

void main()
{
    gl_Position = ortho * vec4(vertex_pos + vec3(pos, 0.0), 1.0);
    uv_coords = uvs;
}